kind: pipeline
type: docker
name: default

steps:
  - name: listing
    image: alpine
    commands:
      - ls
      - pwd
      - cd /data
      - pwd

  - name: clone
    image: alpine/git
    commands:
      - git clone https://github.com/zheng-wei-wu/Drone_test.git
      - pwd
      - ls

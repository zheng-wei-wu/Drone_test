kind: pipeline
type: docker
name: default

steps:
  - name: listing
    image: alpine
    commands:
      - ls
      - pwd
      
  - name: clone_2
    image: alpine/git
    commands:
      - git clone https://github.com/zheng-wei-wu/Drone_test.git
      - pwd
      - ls
